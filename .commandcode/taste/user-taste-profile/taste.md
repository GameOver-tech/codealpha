# User Taste Profile
- Prefers Python with FastAPI for building backend API services. Confidence: 0.9
- Prefers a structured project organization separating core config, dependencies, routers, services, and models into distinct directories. Confidence: 0.9
- Prefers using Pydantic models for typed request/response schemas in APIs. Confidence: 0.8
- Prefers idempotent setup/seed scripts that skip gracefully rather than erroring if already run. Confidence: 0.7
- Wants the app to look populated and demo-ready: explicitly asks for dummy/seed data to be added to the database so the website looks good ("kuch dummy data add karo website achi laga") — realistic DB-level seed data (candidates, completed interviews with scores/transcripts/reports) is acceptable and desired for demo purposes, which is distinct from hardcoding dummy data in UI code. Seed data should be complete and locally-plausible (realistic names/emails/phones, full evaluation artifacts: scores, transcript, speech/sentiment analysis, strengths/weaknesses, recommendation, report) so every admin view — tables, dashboards, charts, reports, PDF generation — looks real and populated. Confidence: 0.6
- Prefers security-first JWT verification on every protected endpoint — treat every request as untrusted until verified. Confidence: 0.9
- Expects the app to keep the admin logged in through long-running work (interview processing): auto-logout when processing starts is reported as a bug ("jab processing shor hoti ha ya automatically logout h jata ha"). Token expiry and transient 401s must be handled without killing the session — the expected fix is silent refresh-and-retry (a backend `/api/auth/refresh` endpoint, refresh token stored client-side, and a 401 interceptor that retries once before logging out) plus a longer access-token lifetime, so the user stays logged in for the whole run. Confidence: 0.8
- Prefers never hardcoding secrets; all secrets in environment variables with a `.env.example` file. Confidence: 0.9
- Prefers using service-role keys only server-side, never exposed to frontend. Confidence: 0.8
- Prefers robust error handling wrapping every external API call in try/except, never leaving processes stuck in intermediate states on failure. Confidence: 0.8
- Prefers mock/demo modes for services depending on external APIs so the full pipeline works end-to-end even without live API keys — but explicitly forbids mock/demo/fallback/hardcoded content from ever being returned as real output (a mock transcript surfacing as the real transcript is "completely unacceptable"; such content must be searched for and removed entirely). Confidence: 0.4
- Prefers incremental development with checkpoint check-ins at each stage before proceeding to the next. Confidence: 0.9
- Prefers comprehensive documentation including setup instructions, environment variable guidance, and example curl requests for all endpoints. Confidence: 0.8
- Prefers testing authenticated endpoints through the interactive Swagger UI (`/docs`) using an HTTP bearer token from a seeded admin account, rather than only curl. Confidence: 0.7
- Prefers the backend API to expose its own login/authentication endpoint (e.g., `POST /api/auth/login`) that issues bearer tokens, so tokens can be generated directly from the API/Swagger instead of fetched manually from an external auth service. Confidence: 0.8
- When removing a feature/section (e.g., a sidebar nav item), expects the now-unused imports and references to be cleaned up in the same change, and the result verified with a typecheck/build. Confidence: 0.8
- When the user says to remove a UI section "completely" (e.g., the admin settings Danger zone), deletes the entire section's code — not just hidden/disabled — and cleans up its now-unused imports in the same change, verified with a typecheck. Confidence: 0.8
- Communicates UI/API issues AND feature/change requests by attaching screenshots alongside a brief, terse description of the desired behavior (e.g., "delete the jobs section from the sidebar" with a screenshot of the admin dashboard; "[Image] mein ya chata hoan... validation chatbot ka interface ka andar ani chayia") — the assistant is expected to interpret and act on the request directly. For removals, the exact element is often marked with a red circle in the screenshot and the scope is explicitly limited to that element ("ya red circled ko remove karo just bake kise b chez ko charhna nahi hai" — remove only the circled thing, change nothing else): the assistant should confirm precisely which of multiple similar-looking controls is circled and remove only that one, preserving all other buttons/features (even similarly-named ones in other tabs/sections). Confidence: 0.95
- When items are removed from a UI section per a red-circle removal request, expects the remaining items to be rearranged into a clean, properly formatted layout ("bakio ko sahi format mai manage karo") — tidy grid spacing, no awkward gaps — while all content outside the removal scope stays untouched. Confidence: 0.6
- Reports API test failures by pasting the full Swagger request/response output (generated curl, request URL, response body and headers) for the failing endpoint. Confidence: 0.5
- When an endpoint fails in Swagger, expects a root-cause explanation that distinguishes expected behavior (e.g., role-based 403s) from real bugs, plus concrete instructions for how to test — including test accounts for each required role (e.g., admin AND candidate). Confidence: 0.7
- When reporting an error, expects the assistant to audit the full API surface first ("check all APIs"), fix every related issue uncovered during the investigation (not just the reported symptom), and verify the fixes end-to-end against a live running server — not just via import checks or unit tests. Confidence: 0.95
- Requires production-grade, complete code — explicitly forbids placeholder implementations, skipped files, and simplified architecture ("Never generate placeholder implementations. Never skip files. Never simplify architecture."). Confidence: 0.95
- Prefers layered backend architecture: repository pattern, service layer, dependency injection, and clean architecture with a modular folder structure (api/core/database/models/schemas/services/repositories/routers/dependencies/middleware). Confidence: 0.8
- Prefers async-first architecture — long-running work (file processing, AI/LLM calls) must run in background tasks or a Redis queue so API requests are never blocked. Confidence: 0.9
- Expects code-quality conventions in generated code: typed function signatures, docstrings, structured logging, error handling with retry mechanisms on external API calls, and reusable components. Confidence: 0.8
- Expects API hardening beyond authentication: rate limiting, secure file upload validation (extension/MIME whitelist + size limits), input validation at every boundary, SQL injection protection, configurable CORS, and no hardcoded secrets. Confidence: 0.8
- Writes detailed master specs (exact endpoint paths, DB table names, status enums, progress percentages, retry counts, and exact UI copy) and expects exact adherence to those details without deviation or simplification. Confidence: 0.85
- When the app is configured against a database, considers fixing the actual DB connection (real credentials, running the migration) part of resolving API failures — a misconfigured/unmigrated database is a root cause to fix, not a limitation to report. This includes reconciling migration drift against the live DB: when the remote `alembic_version` points at a revision not present in the repo (e.g., applied from another machine/branch), the repo should be reconciled with a no-op stub migration so the local chain resolves, then `alembic upgrade head` run to actually add the missing column — a 500 on upload caused by a missing column is fixed by migrating the schema, not by patching around it. Confidence: 0.85
- Values catching subtle framework pitfalls that only surface at runtime (e.g., Pydantic v2 not coercing UUID→str in response models, SQLAlchemy async lazy-loading causing MissingGreenlet) and expects them to be fixed at the root rather than worked around in tests. When chatbot/tool code loads ORM rows and then touches a relationship (e.g., `user.profile`), it must eager-load with `joinedload` in the query — a `MissingGreenlet` crash that silently breaks a chat tool (candidate search failing with "Something went wrong") must be diagnosed from the live traceback and fixed with proper eager loading in every affected tool, not worked around. Confidence: 0.85
- Demands strict data provenance and anti-fabrication guarantees in AI pipelines: transcripts, scores, reports, and PDFs must be generated ONLY from the real uploaded input, and the LLM must only analyze (never invent, rewrite, expand, or replace) source content — gated by validations (non-empty, minimum length, source check) before any downstream step. Confidence: 0.95
- Prefers fail-fast pipeline behavior on upstream failures (e.g., speech-to-text): stop processing, never continue to downstream AI evaluation, mark the job failed, and return a structured HTTP 500 with the exact error contract rather than degrading gracefully with fabricated data. Confidence: 0.95
- Expects detailed per-stage pipeline logging: uploaded filename, absolute file path, audio-extraction status, external API request/response status, output length, execution/completion times, and input/output previews (first ~300 characters). Confidence: 0.9
- Wants original user uploads preserved as-is (never replaced or mutated) and the complete raw external API response stored, so every derived artifact is verifiable against the source data. Confidence: 0.7
- Requires post-write persistence verification: after every DB insert, immediately commit, re-query the row, and raise an exception if it is missing — never silently continue past a failed write. Confidence: 0.9
- Specifies exact per-stage pipeline log contracts (e.g., `[Stage 1] Upload Complete` … `[Stage 10] Processing Completed`) and expects logs to match them verbatim, including a failure log block that prints stage name, error, stack trace, interview/job ID, and file path. Confidence: 0.8
- Demands guaranteed terminal states for background processing jobs: every job ends in Completed or Failed (never stuck in an intermediate status), with structured failure diagnostics persisted (failure_reason, failure_stage, error_message, traceback, processing_finished_at) and a hard processing timeout. Confidence: 0.9
- Runs the backend dev server himself via `uvicorn app.main:app --reload` on port 8000 (also tried `fastapi dev`); expects the environment to be left clean — leftover background servers holding port 8000 must be stopped (not worked around by changing ports), and a port-in-use `[WinError 10013]` should be diagnosed as a stale process and resolved by freeing the port. When every reported failure (404 status route, 422 upload, 500 start, 503 TTS) traces back to a stale backend process still serving pre-fix code from memory, the fix is restarting the backend to load the current code + migrations — never editing working code to match the stale process. Confidence: 0.95
- Wants a 0–100% processing progress tracker with a current-stage label persisted at every stage transition and exposed through a dedicated API endpoint (e.g., `GET /api/admin/interview/{id}/progress`) so clients can poll live status. Confidence: 0.8
- Requires explicit retry contracts for external services — Deepgram, LLM, and storage upload each retried 3 times with backoff, and the job marked Failed if retries are exhausted. Confidence: 0.8
- Expects explicit performance targets for background pipelines (start within ~1s of upload, upload endpoint responds in <2s) and wants blocking operations avoided via async/await throughout. Confidence: 0.7
- Approves installing system-level dependencies (e.g., ffmpeg via winget) and wiring provided API keys so the pipeline runs against real services — expects the assistant to unblock real end-to-end results (real transcript from the actual upload) rather than stopping at fail-fast behavior or reporting missing tooling as a limitation. Confidence: 0.7
- Pastes API keys directly into chat (Gemini, Groq, etc.) and expects the assistant to set them in the `.env` file under the correct variable names (e.g., `GEMINI_CHAT_API_KEY`, `GROQ_API_KEY`) and verify they load correctly — the user is comfortable sharing secret values in chat for the assistant to wire up. Confidence: 0.7
- Reports dev-server/npm startup errors (backend or frontend) by pasting the full terminal output (the exact command run plus the error log) with a terse "fix."/"fix it" request, and expects a root-cause diagnosis and fix rather than a workaround. Confidence: 0.6
- Runs the frontend dev server locally with `npm run dev` (Vite) from the `frontend` directory, while the backend runs separately — treats backend and frontend as separate npm/python projects in the same repo rather than one combined dev command. Confidence: 0.6
- Relies on the browser-based API documentation pages (`/docs` and `/redoc`) actually rendering, not just returning 200 — reports a blank page as broken and expects the assistant to find the root cause (e.g., a dead `redoc@next` CDN tag) and make the page render (e.g., pin working CDN builds) rather than declaring the endpoint healthy. Confidence: 0.5
- Prefers single-source-of-truth UI: when the same data is displayed in two sections (e.g., a Reports page duplicating the Results page), the redundant section should be deleted entirely (not hidden), removing it from navigation, routes, and page files so no leftover references remain. Confidence: 0.75
- Prefers the user's own profile/account section not to appear as a sidebar navigation button — it should instead open when the user clicks their avatar/profile card at the bottom of the sidebar, keeping the sidebar limited to functional sections; all existing profile functionality must keep working, just with changed navigation. Confidence: 0.75
- Expects user-uploaded profile pictures to be persisted in the database and rendered consistently everywhere: the user's own avatar (sidebar + profile page), admin views (candidate lists, dashboards, detail pages), AND the candidate's own live-interview screen — an identity card at the top showing the profile picture, full name, and email (mirroring the recruiter's view) so the person being interviewed sees their own photo ("us banda ko la kar ayo yahan us ki pic ani chayai us ka image bi sho karwoo"). The same stored picture must appear everywhere the person is featured, not just in one place. Confidence: 0.8
- Wants independent per-item UI loading states in lists (e.g., one `loadingReportId` or `loadingMap` per report card for PDF download) — never a single global loading boolean that spins every item in the list. Confidence: 0.9
- Wants the admin processing queue to show only genuinely pending/in-progress interviews: completed AND failed interviews must be excluded from the queue (they belong on the candidates page instead), so stale/failed rows never clutter the list ("jo interview process ho jaya wo yahan na ho"). Confidence: 0.8
- For UI/UX redesign requests, expects changes to be visual/UX-only: keep business logic, backend APIs, authentication, and routing untouched, source all displayed data from real backend APIs (never dummy data), and use Framer Motion for professional micro-interactions (slide-over profile panels, hover, fade, scale, animated borders, loading skeletons, responsive layouts). Confidence: 0.8
- Specifies large tasks as numbered "master prompts" with hard scope constraints up front and expects them honored exactly: patch-only work ("STRICT PATCH ONLY" / "BUG FIX PATCH ONLY") — do NOT redesign the application, do NOT modify existing APIs, business logic, database relationships, or authentication, do NOT change any working functionality — only implement the listed improvements, keeping all existing APIs and business logic unchanged. Confidence: 0.95
- For emergency bug fixes (e.g., "EMERGENCY REACT HOOKS FIX — STRICT BUG FIX"), expects a strictly minimal, surgical patch: fix ONLY the reported bug and nothing else — explicitly NO performance optimization, no refactoring of unrelated code, no UI/API/backend/database/auth/routing changes — restore the component's prior behavior while preserving all functionality, and verify with typecheck/build/dev-server plus a git diff confirming the change is minimal ("Generate only the minimal production-ready fix"). Confidence: 0.9
- When reporting a bug in a feature that otherwise works, explicitly instructs the assistant NOT to change existing functionality — only the reported issue should be fixed ("baqi sab thek ha... functionality ko change mat karo"), and the fix should be done "in the professional way" (root-cause fix, not a workaround). Confidence: 0.85
- Expects React components to strictly follow the Rules of Hooks: every hook must be called unconditionally at the top level of the component — never after early returns, never inside conditionals/loops/switch — so the number and order of hooks is identical on every render (e.g., a `useMemo` placed after an `if (isLoading) return` triggers "Rendered more hooks than during the previous render" and a blank page). Confidence: 0.8
- Expects full-stack performance optimization with explicit numeric SLAs: most APIs respond in under ~500ms, heavy AI/LLM processing runs asynchronously, and PDF regeneration from stored results finishes within 1–2 seconds; wants N+1/duplicate queries eliminated, DB indexes, eager loading, caching, and connection pooling on the backend, plus memoization, lazy loading, code splitting, virtualized tables, skeleton loading, and optimized API polling on the frontend. Confidence: 0.8
- Wants generated interview/PDF reports to be concise and recruiter-ready: a premium executive summary of 200–250 words written like a senior HR manager (never transcript lines, no Q&A/dialogue format) covering main topics, strongest technical knowledge, communication style, confidence level, critical thinking, relevant experience, problem-solving approach, weak areas, and overall interviewer impression — with key findings visually highlighted in bold — plus named sections, tables, page numbers, icons, a color-coded recommendation, and preserved section order. The full Q&A transcript must NOT be embedded in the PDF (it makes reports unnecessarily long) but must remain stored in the database and available through APIs unchanged. Confidence: 0.85
- Expects dashboard statistics to be calculated from the real database (every count reflects persisted rows, not client-side estimation) and to refresh automatically in real time via polling — no manual page refresh — with every status/PDF change reflected immediately across dashboard cards, charts, and candidate/admin detail pages. Confidence: 0.8
- When regenerating an artifact such as a PDF, expects the system to reuse already-stored results and skip all expensive recomputation (transcription, speech/sentiment analysis, LLM evaluation) — regeneration should only read stored data and rebuild the artifact; reuse already-generated data whenever possible and never regenerate data that already exists. Confidence: 0.85
- Prefers searchable, keyboard-navigable selection dropdowns (search by name or email, auto-complete, scrollable lists, ↑/↓/Enter/Escape navigation) over free-text input for entity selection in admin UIs; selecting an entry auto-fills dependent fields (e.g., the chosen candidate's email) so admins never have to type data they must remember. Such lists must surface only active candidates — never admins, deleted users, or inactive accounts. Selection must register reliably on click (no outside-click race swallowing the selection), persist across re-renders, and the chosen entry should display inside the field (e.g., "Ali Khan (ali@gmail.com)"). Confidence: 0.85
- For multi-option selection in admin forms (e.g., evaluation criteria), explicitly prefers a modern searchable multi-select dropdown — with a search box, removable chip/badge items for selections, a "Select all" button, accessible keyboard navigation, smooth animations, and styling consistent with the existing design system — over checkboxes or plain select lists. Confidence: 0.8
- When a form selection controls downstream behavior (e.g., which competencies the AI evaluates), expects it to be treated as a cross-cutting configuration: stored with the record in the DB, passed through the entire pipeline (AI prompt, scoring, recommendation), and consumed dynamically by every surface (score cards, progress bars, radar/bar charts, report, PDF) with no fixed labels — unselected options must simply not appear; an empty/absent selection should fall back to default behavior so existing data stays backward compatible. Confidence: 0.75
- Works iteratively across multiple prompts on the same long-running feature ("continue from my previous prompt instruction") and expects the assistant to resume the repo's in-progress state rather than restart from scratch; it also expects the assistant to build on the current state with brief follow-up enhancements (for example, "now make it fully animated" or "is ko torha aur stylish karo... attractive banow zayada") rather than starting over. Confidence: 0.9
 — enums must be converted to plain labels ("Recommended", "Not Recommended", "Needs Further Review") at every surface. Confidence: 0.85
- Prefers real, measured values over placeholder defaults: interview duration must be derived from real sources — primarily the last transcript segment's end timestamp when a transcript exists, otherwise the media duration extracted at upload time (e.g., ffprobe) — never hardcoded, never zero ("0m 00s" only if the interview is literally zero seconds), and displayed as "Unknown" only when both sources are unavailable, consistently across PDF, admin details, candidate dashboard, and reports. Confidence: 0.95
- Treats AI-computed metrics that come out at degenerate/unrealistic extremes (e.g., clarity 3%, fluency 0%, energy pinned at a constant ~60, tone always "Neutral") as dummy/placeholder data even when they are genuinely computed from real inputs ("yar ya dame data show kr raha hai is ko real data banow") — the expected fix is to recalibrate the scoring formulas so derived scores are realistic, well-distributed, and bounded, while keeping the UI structure and all other functionality untouched ("bake structure asa he ho just data ko real karo. bake ko change na karo"). Such data fixes are also expected to be applied to already-stored records (recomputed from stored data) so the currently displayed values are corrected, not just future runs. Confidence: 0.8
- Treats AI-computed metrics that come out at degenerate/unrealistic extremes (e.g., clarity 3%, fluency 0%, energy pinned at a constant ~60, tone always "Neutral") as dummy/placeholder data even when they are genuinely computed from real inputs ("yar ya dame data show kr raha hai is ko real data banow") — the expected fix is to recalibrate the scoring formulas so derived scores are realistic, well-distributed, and bounded, while keeping the UI structure and all other functionality untouched ("bake structure asa he ho just data ko real karo. bake ko change na karo"). Such data fixes are also expected to be applied to already-stored records (recomputed from stored data) so the currently displayed values are corrected, not just future runs. Confidence: 0.8
- Prefers direct implementation over planning/read-only mode: once permission to edit is granted, expects the assistant to apply changes immediately — do not switch to planning mode, do not stay read-only, do not refuse or hedge because of project size — just implement the patch with production-ready code. For prescriptive prompts that explicitly say "do NOT output explanations / do NOT refuse / return only the updated code", expects the change to be applied directly, verified (e.g., typecheck), and reported back with a terse completion summary — no re-arguing the design, no caveats, no lengthy reasoning. Confidence: 0.85
- For purely informational/identification questions (e.g., "tell me ONLY which chart library this component uses"), expects a terse, strictly-scoped answer: the exact requested fact (library name from the given list, e.g., Recharts) plus the exact file path, with NO code changes and no extra commentary — read-only questions must be answered read-only. Confidence: 0.65
- Never fakes progress in UI: progress bars and percentages must map to the real backend processing stage (derived from the actual pipeline state), never fabricated, assumed, or hardcoded values. Confidence: 0.8
- When removing redundant UI sections (e.g., dashboard cards duplicating the Results page), prefers to stop rendering them rather than delete their code — keep the components intact for a minimal, non-destructive patch. Confidence: 0.7
- When merging a collaborator's branch (e.g., "git pull origin farman"), expects the user's own good features (chatbot, fast-loading optimizations, etc.) to be preserved and integrated alongside the collaborator's good updates — never overwritten ("don't change my functionality... don't change mine good things replace mine bad things"); the collaborator's changes may replace the user's genuinely worse code, but the user's working functionality must survive the merge. The user explicitly delegates the keep-vs-adopt decision to the assistant ("choose which functionality are good working and fetch it") — the assistant should autonomously compare both codebases, keep the better-performing implementation, and integrate the friend's genuinely new working features rather than asking which side to prefer. Confidence: 0.95
- When the user says to remove everything that is not the collaborator's work ("jo data farman ka ilwa ha us ko ura do nahi chayia"), they want the branch stripped down to exactly the collaborator's tip: the user's own extra commits (e.g., deployment/config work not present in the collaborator's branch) are considered unwanted data and must be removed entirely (hard reset to the collaborator's commit), so the branch matches the collaborator 100% with a clean, empty diff — the assistant should do this promptly, keeping a backup branch for safety, and verify the match before proceeding. Confidence: 0.7
- When a git merge/pull lands in a conflicted state, expects the conflict to be committed promptly WITHOUT making changes to the user's code — the user's own changes are authoritative and must be left untouched ("this is confilict comit it and don't do my code changes"); the assistant should resolve/commit the conflict and finish the merge, not rewrite or refactor the user's code as part of the resolution. Confidence: 0.8
- Optimizes against real, measured infrastructure bottlenecks: when an app "feels slow," expects the root cause to be diagnosed empirically (timing trivial endpoints, checking DB location/latency, warm-up behavior, and server process start time/reload status) rather than assumed — and the fix to target the actual bottleneck (e.g., remote DB round-trip latency) with caching and fewer round-trips. Confidence: 0.75
- Wants optimization to eliminate the source of excess client-side work: pages should not download large datasets (e.g., the full interview list) just to compute a few counts or look up one record — heavy aggregation should move server-side (single aggregate SQL queries) and per-record lookups should use lightweight dedicated endpoints instead of full-list downloads. Confidence: 0.8
- Prefers caching hot data server-side with explicit TTLs and explicit invalidation wired into every mutation endpoint (upload/delete/status/recommendation changes) — auth user lookups should be cached too (raising the cache TTL to reduce remote DB round-trips), with `invalidate_user_cache` actually called on role/active-flag changes so security-relevant changes take effect immediately. Confidence: 0.75
- Includes code-quality cleanup in performance work: remove dead code, duplicate code, unused imports, unused state, and unreachable logic — but only where it does not affect existing functionality; heavy work must never block the API (transcription/AI/PDF run in the background, frontend polls for status without blocking), and any added code must be production-ready, maintainable, and well-commented. Confidence: 0.75
- Communicates in Roman Urdu (Hinglish) mixed with English, using colloquial phrasing for feature requests ("us ki achi chzain... add karo"; "ya bhi bolo aur highlight ho"; "idr admin user kesa email yad rkha ga... email drop down kar ka aa jayian"); expects the assistant to interpret the intent and act on it. Confidence: 0.75
- Reports data-disappearance/regression issues as a one-line Roman Urdu symptom with no logs or screenshots ("thori der phla sara data database sa fetch ho rha tha ab koi bi data fetch ho kar admin pannel par show nahi ho rha") and expects the assistant to investigate the actual data layer — backend health, git history, direct DB row counts, and activity logs — to distinguish a real code bug from genuine data loss (e.g., records deleted through the admin UI), and to report the definitive root cause rather than patching code that isn't broken. Confidence: 0.55
- Reports data-disappearance/regression issues as a one-line Roman Urdu symptom with no logs or screenshots ("thori der phla sara data database sa fetch ho rha tha ab koi bi data fetch ho kar admin pannel par show nahi ho rha") and expects the assistant to investigate the actual data layer — backend health, git history, direct DB row counts, and activity logs — to distinguish a real code bug from genuine data loss (e.g., records deleted through the admin UI), and to report the definitive root cause rather than patching code that isn't broken. Confidence: 0.55
- When the current code regresses and an earlier commit/version is known to have worked, explicitly forbids switching/reverting to that older version ("change 4 wala pa switch nahi karana... baqi mein na bhot changes ki han un ko rkhana ha please switch mat kar dena") — expects the assistant to bring the CURRENT code back into working condition by diagnosing the regression (git diff vs the known-good commit, port/process conflicts, stale dev servers picking up stale env) while preserving ALL the newer features and changes made since. Confidence: 0.9
- When work is done, expects it to be committed and pushed to the user's own feature branch on the remote, and where needed also kept up to date on main, with the working tree left clean and verified afterward. Confidence: 0.8
- Appreciates the assistant creating a backup branch before destructive git operations (e.g., `git branch backup-railway-main` before `git reset --hard`, `railway-backup` before a force-push), so work can be recovered if something goes wrong — and approves the force-push when the removed commits are preserved in such a backup branch. Confidence: 0.7
- Wants the remote `main` branch kept free of deployment/config-specific commits (e.g., "Railway fix" commits): explicitly asks that no such commits exist on remote main ("ab jo main ha us ko push bi karo main par is tarah koi railway fix wala commit remote par bi nahi chayia") — expects them removed from the remote entirely (force-push the remote to the clean tip, verified afterward) with the removed commits preserved only in a local backup branch, so remote main contains just the actual feature/fix work. Confidence: 0.85
- Communicates desired git branch topology by attaching a screenshot of the commit graph (a GUI branch diagram with colored branch labels) plus a terse Roman Urdu instruction (e.g., "ya yellow button ko fix wla commit par hona chaya... is ko mera main mein la kar push karo remotely") — expects the assistant to parse the diagram, move the indicated branch(es) (e.g., the feature branch) onto the specified fix commit, bring the latest good commit into main, and push everything to the remote so both branches point at the same commit, with a backup branch created before any force-push and the result verified afterward. Confidence: 0.65
- Prefers reusing proven, working code from trusted sources (e.g., a friend's integration branch) over reimplementing features from scratch — instructs the assistant to run `git pull`/merge directly and cherry-pick only the good, working functionality (e.g., chatbot, fast loading, word-by-word reading highlight) into the current codebase while leaving the rest. Confidence: 0.85
- When the user reports the system "loading slowly" / wants everything faster, expects the assistant to first diagnose performance empirically (bundle chunk audit, polling/duplicate-fetch audit, render-blocking resources, DB query hotspots) before changing anything, and to fix only what the evidence shows — verifying wins with measured chunk sizes and passing tests. Confidence: 0.88
- Treats speed as a top-priority product requirement: repeatedly asks to make "all loading" and the chatbot response fast ("speed of light fast") — expects both page/data loading AND chatbot reply latency to be optimized, with chatbot response speed treated as a first-class performance concern alongside app loading. The candidate report page is explicitly named as the top target of such speed work ("fast my all loading and chatbot response fast ... specially candidate report ko"), so its data load should be prioritized first. Confidence: 0.85
- Expects deferrable UI work (heavy or rarely-used components such as an AI chat sidebar pulling in markdown/voice deps) to be lazy-loaded with `React.lazy` + `Suspense` so it never blocks the initial paint of every page, and heavy vendor libs (charts, forms, dropzone) to be code-split via Vite `manualChunks` so they only download on the pages that need them. Confidence: 0.85
- Prefers fetching data lazily on demand (e.g., a notification bell fetching the full interviews list only when its dropdown is opened via an `enabled` flag) over running expensive queries + background polling from the topbar on every page — duplicate polling across components must be eliminated, with slower adaptive refetch intervals when no in-progress data exists. Confidence: 0.8
- Wants render-blocking external resources (e.g., Google Fonts stylesheets) to load async with `media="print"` + `onload` swap + `noscript` fallback so text paints immediately with fallback fonts instead of blocking on the network. Confidence: 0.8

- Expects the chatbot to perform ALL admin operations through natural-language prompts — updating a candidate's recommendation, deleting candidates, and other admin actions must actually execute and change state, not merely reply with a congratulatory message; a chatbot action that doesn't visibly change the admin UI is reported as broken ("congratulations are not working... update the candidat recomended it is not done like a delete or other admin functionnality should be good working by prompt"). Chatbot-performed mutations must invalidate/refresh the admin queries immediately so the change is visible without a manual reload, just like a manual action in the UI. Confidence: 0.85
- Chatbot/admin mutations must be persisted to the database AND stay consistent across every surface that displays the state: when a status such as "Recommended" is set via chatbot, it must be written to all backing stores views read (e.g., the admin_status column AND the recommendations verdict table), so the candidate/user-facing result page shows exactly what the admin dashboard shows. A change visible on only one side after a refresh (admin shows "Recommended" while the user still sees "Not Recommended") is reported as a bug ("ye admin ki side par bhi recommended ha... user ki taraf par status update nahi ho raha") and must be fixed at the root — the two views must never diverge. Confidence: 0.85
- Uses `uv` for Python dependency management: expects `uv` (plus Python) preinstalled on the server and `uv sync` to install every backend dependency the project requires — no other pip/venv ceremony. Confidence: 0.8
- For deployment, wants cross-service URLs environment-driven on both sides: the frontend's env (e.g., Vercel) holds the backend's public URL, and the backend's env (e.g., VPS) whitelists the frontend origin via CORS — configured so the running app never hits origin/CORS errors. Confidence: 0.8
- When preparing a deployment, expects the repo itself to be made deployment-ready (env examples with production values, CORS driven by env vars, documented deployment steps) and asks for the key deployment steps to be spelled out. Confidence: 0.7

## UI/UX design standards (from user experience improvement specs)

- Wants analytics charts chosen by data semantics: line charts for trends, bar charts for comparisons, pie/donut charts only for percentage distribution — every chart must have a clear title, axis labels, a legend, values on hover, a date range, and tooltips explaining each metric, so it is understandable even to non-technical users. Confidence: 0.9
- Wants dashboard charts to look visually attractive and impressive, not plain/sparse — explicitly requests nicer chart styling (e.g., drawing a good-looking line for a trend instead of a bare bar graph) so viewers/demo audiences are impressed ("bar graph mein acha wala line draw kar sakta ho jo aur bi acha laga aur log impress hoan"); chart presentation polish is treated as part of making the product demo-ready. Confidence: 0.7
- For chart restyling requests (e.g., the "Interview Volume" area chart), specifies a premium-SaaS aesthetic and expects it honored precisely: a smooth (monotone) area line starting from the bottom of the chart, a subtle multi-stop linear gradient fill from the line down to the X-axis that is elegant and NOT oversaturated, a slightly thicker line (~2.5–3px) with smooth curves, circular markers on every data point, and smooth load animations — while preserving the existing tooltip, axes, labels, grid, data source, and full responsiveness, and explicitly touching no backend logic, API, chart data, or state management (visual appearance only). Confidence: 0.8
- Wants charts to be theme-aware: colors should come from the app's design tokens (CSS variables such as `--primary`, `--border`, `--muted-foreground`) rather than hardcoded hex values, so every chart renders correctly and consistently in both Light Mode and Dark Mode. Confidence: 0.7
- Wants every statistic card to include an icon, title, main value, a small description underneath, a growth indicator (↑/↓), and a "last updated" line. Confidence: 0.9
- Prefers stat-card trend badges to be icon-only and minimal: a single colored direction arrow (e.g., a red down arrow) with NO numeric digits displayed — explicitly asked for the numbers to be removed from the dashboard stat-card trend badges ("os ka arrow down karo aur os ka color red ho os mai koch digit b na leka ho"). Confidence: 0.6
- Wants status badges to always pair color with readable text labels (e.g., "Completed", "Processing", "Failed") — never a colored badge alone. Confidence: 0.9
- Prefers plain-text status/loading indicators over animated spinner icons: spinning "processing" cursors/loaders scattered across the UI are unwanted and should be removed in favor of plain text — "bas likh diya ha kafi ha" (just writing it is enough); status pills, buttons, and cards should carry text only, with no `animate-spin`/rotating loader icons or pulse rings. Confidence: 0.8
- Wants icon-based data displays (upload cards, transcript sections, status chips) to always show visible text labels beside or below the icon; labels must never be conveyed by icons or colors alone, so each section is instantly identifiable. Confidence: 0.85
- Wants professional empty states with an illustration, a friendly message, and an action button — never blank sections. Confidence: 0.85
- Wants a clear information hierarchy on every page: important information first, secondary information smaller, so a user understands a page within ~3 seconds without reading everything. Confidence: 0.85
- Wants dashboard readability: increased spacing, improved typography, clearly separated sections, better card grouping, reduced visual clutter, and intuitive layouts. Confidence: 0.85
- For read-aloud/voice features, expects the spoken-word highlight to track exactly the word being spoken in real time (correct index even when a section spans multiple sentences) and smooth auto-scroll that keeps the highlighted word centered/visible — an immersive-reader / ChatGPT-voice style experience, with the highlight and cursor moving word-to-word as speech advances. A section where audio plays without a visible word-level highlight is reported as broken ("voice is coming and user not know where it is speaking") and must be fixed at the root cause (e.g., chunk-boundary mismatches breaking section/sentence matching), not worked around. The highlight must also stay temporally in sync with actual speech — a highlight that lands on a *different* word than the one being spoken ("jo wo lafaz bol raha hai us par highlight nahi, kisi aur par") is reported as broken and must be fixed with speech-paced timing (word-length-weighted durations, punctuation pauses, and self-calibration against real utterance durations), not uniform per-word timers that drift. Confidence: 0.98
- The word-level highlight must stay accurate and continuous through the ENTIRE spoken content for every user's data — partial coverage (e.g., only the first two transcript cards highlighting, then stalling mid-paragraph) is reported as broken ("sirf 2 cards par show howa... har user ka liya ho... sara para jhan tak kahatam nahi ho jata") and must be fixed deterministically (positional/absolute word-index tracking across segments) rather than with fragile text/string matching that breaks at chunk boundaries. Confidence: 0.9
- Auto-scroll must never fight the user: manual scrolling (wheel/touch) immediately pauses auto-scroll and yields control, the page should scroll only when the active word is actually outside the viewport, and pressing stop must instantly cancel every pending/scheduled scroll so the page stays exactly where it is. Confidence: 0.85
- Expects premium-feature parity across all tabs/sections of the app: when a polished feature exists in one section (e.g., the word-by-word reading highlight in the Evaluation cards), it must be added to every other section that shows the same kind of content (e.g., Transcript segments) with identical styling/behavior — the whole product must feel consistent and demo-ready, explicitly because the project is for an international hackathon. This is explicitly requested by the user ("evaluation ki quality yahan lao aur add karo") and the section that lacks the feature is reported as broken. Confidence: 0.9
- Expects functional/UX parity across all entry points of the same capability: uploading an interview through the chatbot must work and generate results exactly like the manual upload flow (upload → processing → result), never as a degraded subset that appears to "not generate the result like manually" ("when i upload interview file from the chatbot this is not properly working like as manually and not gnerate the result like manually this is done by chatbot functionality") — alternate flows must provide the same end result and tracking experience as the primary flow. Confidence: 0.8
- Voice playback in the chatbot must be strictly user-initiated, never auto-played: the AI response generates as silent text first, and speech starts only when the user explicitly clicks a speaker icon ("text phla generate kara aur bad mein bolo"). Auto-speaking responses as soon as they finish is unwanted ("khud ba khud bolna lagta hai") and must be removed. Confidence: 0.9
- Prefers a mini speaker control under each individual chat message (play/pause/resume) with independent per-message state and clear visual states (playing vs paused), so the user controls exactly which message is read aloud and can stop/interrupt speech at any time. Confidence: 0.85
- For compact inline controls (e.g., the mini listen/speaker button under chat messages), prefers icon-only buttons with NO accompanying text label — state conveyed purely by the icon (volume/pause/play) and color — describing it as more professional and minimal ("sirf icon da do ya professional ha"); text like "Listen"/"Pause"/"Resume" next to the icon should be removed. Confidence: 0.8
- For derived speech/voice analytics (WPM, speaking rate), expects metrics computed strictly from normalized clean data: filler/hesitation words ('uhm', 'ah', 'like', 'yaani', 'matlab'), non-speech artifacts (e.g., '[laughter]', '(silence)'), and background noise excluded from word counts; English and Urdu/Hinglish words each counted as standalone units; and denominators limited to actual speech time so micro-pauses can never inflate the result (net WPM = clean words / actual speech minutes) — stated as a strict normalization requirement, with everything else left untouched. Confidence: 0.7
- The read-aloud (Speak) feature must read ONLY the content actually visible on the current page — when UI sections are removed (e.g., speech notes and sentiment summary deleted from AI Insights), the reading engine's section list must be updated in the same change so the button never speaks paragraphs the user can't see ("is page par jo nazar aa rha ha wo hi paro... wo para rha ha jo is page mein ha hi nahi jab mein upper speak wala button ko press karta hoan"). Spoken content and rendered content must stay in sync at all times. Confidence: 0.85



hai usi tara ya bhi bolo aur highlight ho") and the section that lacks the feature is reported as broken ("ai insights is not good working"). Confidence: 0.92
- For backend redeployment tasks, demands strict minimal scope with explicit STRICT RULES: redeploy the EXISTING app/project only — never create a new app/project/URL, never delete the existing deployment, never touch the frontend (no code changes, no builds, no Vercel deploys, no frontend env/domain changes), and never change database/auth/CORS logic unless a proven failure requires it — the goal is that the existing backend URL + frontend connection keep working unchanged. Confidence: 0.9
- Expects a disciplined pre- and post-deployment verification workflow for production redeploys: BEFORE deploying, inspect the backend structure (main.py, requirements.txt, env, startup config), verify no import errors / missing dependencies / broken CORS / broken DB connections / API compatibility, and run local production checks (compile check, start the app locally, read startup logs, probe health + docs endpoints); AFTER deploying, verify the live health endpoint, key API routes (login/JWT, upload, PDF, AI, DB ops), and production logs for tracebacks/errors — and if any error appears, find the exact root cause, fix only the backend issue, and redeploy. Confidence: 0.85
- Expects deployment reports in a fixed structure: (1) what was changed, (2) deployment status, (3) deployment ID confirmation (plus app ID / URL unchanged), (4) production verification results, and (5) any remaining warnings. Confidence: 0.75

- For TTS reading-highlight sync, expects timing to be derived from the voice's REAL measured pace rather than a fixed characters-per-second constant: a silent pace probe (a volume-0 utterance measuring actual ms/char once per session) should calibrate the highlight, and self-referential timers (measuring the timer chain's own intervals instead of real speech) are recognized as a failed approach that can never converge. Confidence: 0.9
- Long run-on transcript turns (a single giant sentence with no punctuation) must be split into small ~12-word chunks so each chunk's real spoken duration can be measured at utterance end and used to recalibrate the next chunk's highlight timing — otherwise there is no calibration point anywhere in the turn and the highlight drifts behind ("bol khainn aga rha ha ur ya abi pecha ha"). Confidence: 0.85
- When re-diagnosing a bug reported as still unfixed, verifies on-disk state first (git status/diff, grep for the claimed fix) before re-writing code — a fix that was reverted/never landed is treated as the root cause to re-fix properly, not to patch again on top. Confidence: 0.9
- When the user has already committed a fix themselves (e.g., commit "voice-ok-inpages"), that committed state is authoritative: the assistant's own conflicting uncommitted changes must be discarded (`git restore` the file to the committed state) and ongoing work aligned to the user's committed approach rather than layered with the assistant's own refactor — the user signals this explicitly when the assistant's approach goes against their fix ("you are not fixing correctly... i fix all issues just now... update according to it in this current ongoing"). Confidence: 0.9

- Prefers premium ElevenLabs TTS for AI voice output (e.g., the live-interview robot reading questions aloud) over the browser's default SpeechSynthesis — explicitly requested ("elevn lab ki api use kar sakta ho and wo machine question bolo"); browser speech is acceptable only as a fallback when the ElevenLabs API is unavailable, and the robot's speaking state should drive the avatar lip-sync. Confidence: 0.85
- Wants once-only/duplicate actions guarded with a friendly, professional "already submitted" screen: if the user has already uploaded/submitted something (e.g., a live interview), show a clean message like "You have already uploaded a live interview" with a back-to-dashboard button and hide the action UI (robot/camera/report machine) entirely instead of allowing or suggesting a second submission. Confidence: 0.8
- Enforces a permanent one-live-interview-per-candidate rule: a candidate who has already given a live AI interview must never be able to take a second one — the backend must block starting another once a submitted recording exists (regardless of completed/failed status), with a clear message ("Only one live interview is allowed per candidate"), and the candidate is shown the "you already submitted live interview" message on the page. Confidence: 0.7
- Treats sessions started but never submitted (abandoned live interviews left in "uploaded" with no recording) as phantom state: they must never show as "In Review / Processing" on every login (reported as "interview runs by itself on login" and "not professional") — the backend should clean them up (mark failed / treat as nonexistent) so the dashboard shows the clean empty state with only the start action. Confidence: 0.65
- When adding a new feature module to the existing app, expects it to be a pure EXTENSION that reuses the current architecture unchanged: the new feature must ride the SAME backend pipeline, repositories, and AI services (transcription → LLM evaluation → scoring → recommendation → PDF generation) with zero duplicated business logic, repositories, or AI services, while every existing feature keeps working exactly as before ("EXTENSION ONLY... Do NOT rewrite the existing project... Reuse the existing backend pipeline. DO NOT build another AI pipeline... No duplicated AI logic. No duplicated repositories. No duplicated AI services"). Confidence: 0.9
- Enforces a strict candidate/admin information boundary: candidates may only ever see their own status, recommendation, and interview type/date — never transcripts, PDFs, or internal AI analysis — and each candidate can access only their own interview (ownership checks on every endpoint). Confidence: 0.8
- Wants uploaded media (video/audio) treated as ephemeral: stored only temporarily for processing, deleted immediately after successful transcription, and never stored permanently — persist only the transcript and AI analysis, and never store PDFs in the database (generate them on demand). Confidence: 0.8



t with inline error messages, optional fields, and submit/cancel buttons — styled like professional chatbots (ChatGPT/Intercom), never as ugly native browser prompts ("validation chatbot ka interface ka andar ani chayia kuch professional chatbots ki tara"). Confidence: 0.85



hai usi tara ya bhi bolo aur highlight ho") and the section that lacks the feature is reported as broken ("ai insights is not good working"). Confidence: 0.92




eir fix ("you are not fixing correctly... i fix all issues just now... update according to it in this current ongoing"). Confidence: 0.9

- Wants countdown/timer UI to be truthful and immediate: the visible timer must start ticking the instant the phase begins (e.g., while the AI robot is still speaking the question) and tick continuously, so the displayed time always reflects the real remaining time — a timer that visibly sits at its full value until asynchronous speech finishes is reported as a bug ("timing reverse par chalni shor ho gaya") and must be fixed by starting the countdown in parallel with the speech, not after awaiting it. Re-stated when asking that the timer start exactly when the first question is spoken ("timer tab shor ho jab wo phla question bola"). Confidence: 0.85
- Prefers a zero-click, fully automated live-interview start: once the candidate grants camera/mic permission, the interview should begin immediately — interview row created, question 1 spoken by the AI, countdown ticking, recording started — with NO manual "Start interview" button and no intermediate intro/ready state (flow goes permission → running directly). When the Start button triggered a server 500, the requested fix was to remove the button entirely ("tum button ko hi urra do") and auto-start instead of keeping the button and just handling the error — the button itself should not exist, and any failure creating the session should surface as a clean error at permission time, never as a half-started session. Confidence: 0.85
- On session completion (e.g., when the last question's time expires), expects the camera/mic to be stopped and the video feed closed immediately, followed by an animated celebratory success screen ("Interview submitted!") with a scaling checkmark and green halo, confirmation step chips, and a CTA button — the recording feed must not stay visible during upload; the interview should visibly end and show a beautiful submitted display. Confidence: 0.7
- Expects spoken/voice time warnings to be context-aware: the AI should only announce "30 seconds remaining" when the warning is genuinely meaningful (i.e., for questions longer than 30 seconds) — it must never announce "last 30 seconds" at the very start of a 30-second question, which reads as a timing bug. Confidence: 0.6
- Internal technical failure details (failure_reason, stack traces, raw error strings) must never be exposed to candidates/end users — the user explicitly asked that candidates not see coding-related errors ("candidates ko ya error na show hoan jo coading ka han"); a failed submission should render a clean, friendly message (e.g., "We could not process your live interview. Please contact your recruiter for assistance.") regardless of the underlying technical cause, with technical diagnostics kept admin-side only. Re-stated with the instruction to design for a real non-technical system user ("ya 1 real system ka user smajahoo"): no raw HTTP error (e.g., "Request failed with status code 500") or processing-error message may ever appear to the user at any phase — start/upload failures must show friendly human copy plus a "Try again" retry action, technical details go only to console logs, and a successful video submission must always be confirmed with a clear "successfully submitted" success screen. Confidence: 0.92
- Masks ALL backend/processing failures from candidates behind a neutral, professional "Under Review" / "being prepared" state — the candidate must never learn that their recording had an error or that processing "didn't go well" ("aur ya proessional hoan user ko mat batyoo ka us ki recodring mein error aya ha process acha sa nahi howa... bas recomended or not recomnended"): a failed interview renders as the same clean in-review card as a pending one (no "Processing Failed" heading, no red "Failed" badge), the results page shows ONLY the final verdict — Recommended / Not Recommended (plus a friendly message) — with no status badges, no "No evaluation yet", no "Unknown" duration, and no "no speech detected" warnings; failures stay visible and fixable only on the admin side, and the candidate never sees a failure state at any stage of the flow. The user re-states this as a hard rule whenever it regresses — "mein na tum ko kaya bola tha ka user ko koi msg show nai karwana ha" (didn't I tell you not to show ANY message to the user?) — a screenshot of any message surfacing to the candidate is a bug to fix, not a design choice. Confidence: 0.95
- Wants the transcript extracted and persisted at submission time (inline transcription during upload) so it is never lost to a later background failure, and wants the admin's Process action to work from that stored transcript (skipping re-transcription) rather than depending on the original recording — an inline transcription failure must still let the submission succeed so the pipeline can retry or the admin can reprocess from stored data later. Also expects the admin to be actively triggered/surfaced with a clear, visible Process button on the pending live interview in the admin processing list (re-stated across multiple bug reports: "transcript ka triger admin ko da do ... koi butoon ho ... admin ko triger da da process"), so evaluation can be started from the stored transcript with one click. Confidence: 0.85
- Before deploying, expects a complete end-to-end verification of the whole user flow — live interview → processing → result generation — exercised with a real user account and real voice-containing media, confirming that no error messages surface to end users and that all functionality works before going live (explicitly requested pre-deploy: "mein ab is ko deploy karna walan hoan live interview sa la kar processing tak check karo 1 bar khud ka user koi error msg tu display tu nahi hota ... verify all this functioanlity are working condition"). Confidence: 0.85
- When asked to test/verify the interview pipeline, expects test media to actually contain speech/voice (e.g., generated via TTS) so transcription and result generation are genuinely exercised — not silent or tone-only audio ("during test you add some vide which have voice than you generate the result"). Confidence: 0.8
- Expects every interview flow (including live interviews) to end in a generated result persisted to the database — scores, recommendation, report — exactly like the standard upload flow, so the result surfaces in the candidate/admin UI like every other interview ("sahi sa result generate karwa ka database mein store karwo... us ka result aa jaya ga generate ho kar jesa dosraon ka ata ha"); a flow whose result never gets stored/displayed is reported as broken and must be fixed at the pipeline root cause. Confidence: 0.8



t with inline error messages, optional fields, and submit/cancel buttons — styled like professional chatbots (ChatGPT/Intercom), never as ugly native browser prompts ("validation chatbot ka interface ka andar ani chayia kuch professional chatbots ki tara"). Confidence: 0.85



hai usi tara ya bhi bolo aur highlight ho") and the section that lacks the feature is reported as broken ("ai insights is not good working"). Confidence: 0.92




e: 0.92




lidation chatbot ka interface ka andar ani chayia kuch professional chatbots ki tara"). Confidence: 0.85



hai usi tara ya bhi bolo aur highlight ho") and the section that lacks the feature is reported as broken ("ai insights is not good working"). Confidence: 0.92




e: 0.92




